$(function() {
    $('.lumpy-color').lumpy({
        begin: '#fff',
        end: '#444',
        steps: 60
    });
    $('.lumpy-direction').lumpy({
        begin: '#444',
        end: '#fff',
        steps: 60,
        direction: 'begin'
    });
    $('.lumpy-more-color').lumpy({
        begin: '#ff87a3',
        end: '#00fa64',
        steps: 100,
    });
});
